/*Project 07Keyboard InstrumentArduino Starter KitUsing a photoresistor and a piezo element to make a light-based thereminPedro RuizMay 29th 2018*/// set up an array with 6 integersint buttons[6];// give the first element of the array the value 2//int buttons[0] = 2;// create an array of frequenciesint notes[] = {262,294,330,349};void setup() {  // begin serial communication  Serial.begin(9600);}void loop() {  // read the analog value and send it to the serial monitor  int keyVal = analogRead(A0);  Serial.println(keyVal);    // use and if...else statement to determine what note to play  if(keyVal >= 1011 && keyVal <= 1023) {    tone(8, notes[0]);  }    else if(keyVal >= 990 && keyVal <= 1010) {    tone(8,notes[1]);  }    else if(keyVal >= 505 && keyVal <= 515) {    tone(8,notes[2]);  }    else if(keyVal >= 5 && keyVal <= 10) {    tone(8,notes[3]);  }    else {    noTone(8);  }  }